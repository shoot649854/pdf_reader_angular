This will list all the buckets in your current project. You'll see the names of the storage buckets, along with additional information like their location and creation time.

```bash
poetry export --without-hashes --format requirements.txt --output requirements.txt
```

```bash
gcloud storage buckets list
gcloud storage buckets list --format="value(name)"
gcloud storage ls gs://visa-pdf
gcloud storage ls 'gs://shoto_test_bucket/USCIS/**/*'
gcloud storage rm gs://visa-pdf/
```

```bash
gcloud storage buckets create gs://new-bucket-name --location=your-region
gcloud projects describe vaulted-zodiac-253111 --format="value(projectNumber)"
```

```bash
gcloud functions logs read process-pdf --region=us-central1
gcloud functions logs read process_pdf --region=us-central1 --limit=50
```
